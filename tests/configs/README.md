# nichtparasoup crawler tests

here are prepared ini files for debugging and testing

run from nichtparasoup root:
```bash
./nichtparasoup.py -c tests/configs/<crawler>.ini
````

find detailed log in `tests/logs/`