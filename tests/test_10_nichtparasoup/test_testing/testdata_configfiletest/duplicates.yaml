## this is a test config file for nichtprasoup (v2.x)
## purpose: check if duplicate crawlers are detected

webserver:
  hostname: "0.0.0.0"
  port: 5000

imageserver:
  crawler_upkeep: 30

crawlers:
  - name: "Echo"
    config:
      image_uri: 'test'
  - name: "Echo"
    config:
      image_uri: 'test'
