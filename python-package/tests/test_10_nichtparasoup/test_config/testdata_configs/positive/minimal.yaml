## this is a test config file for nichtprasoup (v3.x)
## purpose: check if minimal settings is enough.

webserver:
  hostname: "0.0.0.0"
  port: 5000

crawlers:
  - name: "MockableImageCrawler"
    config:
      foo: "bar"
