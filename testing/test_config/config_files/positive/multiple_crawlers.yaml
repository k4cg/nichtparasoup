## this is a config file for nichtprasoup
## purpose: test if multiple crawlers are possible

webserver:
  hostname: "0.0.0.0"
  port: 42

imageserver:
  crawler_upkeep: 23

crawlers:
  - type: "Dummy"
    weight: 1
    config:
      image_uri: "test1"
  - type: "Dummy"
    weight: 33
    config:
      image_uri: "test2"
  -
    type: "Dummy"
    weight: 7
    config:
      image_uri: "test3"
