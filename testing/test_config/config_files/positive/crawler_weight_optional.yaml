## this is a config file for nichtprasoup
## purpose: test if optional weight values are filled as expected

webserver:
  hostname: "0.0.0.0"
  port: 5000

imageserver:
  crawler_upkeep: 30

crawlers:
  - type: "Dummy"
    weight: 1
    config:
      image_uri: "test1"
  -
    type: "Dummy"
    # optional `weight` was left out
    config:
      image_uri: "test2"
